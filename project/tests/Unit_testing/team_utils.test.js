// const DButils = require('../../routes/utils/DButils');

// const teams_utils = require('../../routes/utils/teams_utils');


// test('positive test getTeamName', async () => {
//     const teamId = 1;
//     // const teamName = await teams_utils.getTeamName(teamId);
//     expect(await teams_utils.getTeamName(teamId)).toBeTruthy();

// });

// test('negative test getTeamName id doesnt exist', async () => {
//     const teamId = 0;
//     // const teamName = await teams_utils.getTeamName(teamId);
//     expect(await teams_utils.getTeamName(teamId)).toThrowError();
//     // expect(await teams_utils.getTeamName(teamId)).toBeFalsy();

// });

// test('positive test getTeamVenue', async () => {
//     const teamName = 32223;
//     // const teamVenue = await teams_utils.getTeamVenue(teamName);
//     expect(await teams_utils.getTeamVenue(teamName)).toBeTruthy();

// });

// test('negative test getTeamVenue name doesnt exist', async () => {
//     const teamName = 32223;
//     // const teamVenue = await teams_utils.getTeamVenue(teamName);
//     expect(await teams_utils.getTeamVenue(teamName)).toThrowError();
//     // expect(await teams_utils.getTeamVenue(teamName)).toBeFalsy();

// });

// // DButils.disconnectDB()